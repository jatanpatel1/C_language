// 8. Find circumference of Rectangle formula : C = 4 * a

#include<stdio.h>
int main(){
	int length,width;
	printf("Enter the value of width: ");
    scanf("%d", &width);
    printf("Enter the value of length: ");
    scanf("%d", &length);
    printf("circumference of Rectangle, P = %d",2*length+2*width);
    return 0;
}

